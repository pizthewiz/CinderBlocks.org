{
  "name": "CinderBlocks",
  "version": "0.0.0",
  "description": "Catalogue of CinderBlocks",
  "scripts": {
    "postinstall": "([ -d data ] || mkdir data) && ([ -d dist ] || mkdir dist) && ([ -d app/data ] || ln -s `pwd`/data app/data) && ([ -d dist/data ] || ln -s `pwd`/data dist/data)"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pizthewiz/CinderBlocks.org.git"
  },
  "dependencies": {
    "async": "^0.9.0",
    "bower": "^1.3.12",
    "cheerio": "^0.18.0",
    "compression": "^1.3.0",
    "del": "^1.1.1",
    "express": "^4.10.7",
    "gulp": "^3.8.10",
    "gulp-awspublish": "0.0.24",
    "gulp-concat": "^2.4.3",
    "gulp-connect": "^2.2.0",
    "gulp-jshint": "^1.9.0",
    "gulp-minify-css": "^0.3.11",
    "gulp-minify-html": "^0.1.8",
    "gulp-rename": "^1.2.0",
    "gulp-uglify": "^1.0.2",
    "gulp-usemin": "^0.3.11",
    "octonode": "^0.6.13",
    "request": "^2.51.0",
    "xml2js": "^0.4.4"
  },
  "engines": {
    "node": "0.10.x"
  },
  "private": true
}
